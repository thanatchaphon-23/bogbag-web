<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sign up</title>
  <%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "reload" %>
</head>
<body class= "signup_body">
  <div class="signup-container">
    <h2 class="signup-title">Sign up</h2>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: "signup-form" }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="signup-form-group">
        <%= f.label :email, "Email", class: "form-label" %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-input" %>
      </div>

      <div class="signup-form-group">
        <%= f.label :username, "Username", class: "signup-form-label" %>
        <%= f.text_field :username, autofocus: true, autocomplete: "username", class: "signup-form-input" %>
      </div>

      <div class="signup-form-group">
        <%= f.label :password, "Password", class: "signup-form-label" %>
        <% if @minimum_password_length %>
          <small class="signup-form-hint">(<%= @minimum_password_length %> characters minimum)</small>
        <% end %>
        <%= f.password_field :password, autocomplete: "new-password", class: "signup-form-input" %>
      </div>

      <div class="signup-form-group">
        <%= f.label :password_confirmation, "Confirm Password", class: "signup-form-label" %>
        <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "signup-form-input" %>
      </div>

      <div class="signup-form-actions">
        <%= f.submit "Sign up", class: "signup-button signup-primary" %>
      </div>
    <% end %>

    <div class="signup-links-container">
      <p>Already have an account?  <%= render "devise/shared/links" %> </p>
     
    </div>
  </div>
</body>
</html>
