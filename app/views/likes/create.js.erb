const likeSection = document.querySelector("#post-<%= @post.id %> .like-section");
likeSection.innerHTML = `
  <p class="likes-count"><%= @post.likes.count %> Likes</p>
  <%= button_to "Unlike", post_like_path(@post), method: :delete, remote: true, class: "like-button unlike" %>
`;
